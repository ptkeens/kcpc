// this should be the source of truth for frontend paths

export const paths = {
    home: () => "/",
    auth: {
        login: () => "/login",
        register: () => "/register",
        logout: () => "/logout",
    },
}
